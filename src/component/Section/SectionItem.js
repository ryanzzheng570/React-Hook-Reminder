import React from 'react'

const SectionItem = ({itemName, itemDescription}) => {
    return (
        <div>
            <h4>{itemName}</h4>
            <h5>{itemDescription}</h5>

        </div>
    )
}

export default SectionItem
