{
    "name": "react-sushi-restaurant-online",
    "version": "1.0.0",
    "description": "React-Express application for sushi restaurant",
    "main": "server.js",
    "scripts": {
        "heroku-postbuild": "cd client && npm install && npm install --only=dev --no-shrinkwrap && npm run build",
        "start": "node server.js"
    },
    "license": "MIT",
    "devDependencies": {},
    "dependencies": {
        "body-parse": "^0.1.0",
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "path": "^0.12.7"
    }
}